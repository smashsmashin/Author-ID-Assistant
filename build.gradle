buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.3.1' // use compatible version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
